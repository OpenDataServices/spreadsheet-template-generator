# Example

This is an example of a codelist documentation page that is compatible with the tool. If a field references the currency codelist, the tool generates a link to the [currency](#currency) subheading.

## Currency

```{csv-table}
:header-rows: 1
:file: ../example/codelists/currency.csv
```